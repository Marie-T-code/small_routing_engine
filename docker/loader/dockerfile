FROM ubuntu:22.04

RUN apt-get update \
 && DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
      gdal-bin \
      postgresql-client \
      ca-certificates \
      bash \
 && rm -rf /var/lib/apt/lists/*

WORKDIR /work